# خطة إغلاق ما تبقّى في ACMS

> وُضعت: **٢٠٢٦-٠٨-١٥** — بناءً على فحص الكود لا على `PROGRESS.md` وحده.
> القاعدة الثابتة تبقى كما هي: **لا يُعتبر أي شيء منجزًا قبل إثباته شغّالًا على السيرفر** عبر `scripts/smoke.sh`.

---

## ٠. تصحيحات على `PROGRESS.md` قبل البدء

فحص الكود كشف ثلاث نقاط غير مطابقة لما هو مسجَّل. تصحيحها أولًا، لأن خطة مبنية على حالة خاطئة تهدر وقتًا:

| ما هو مسجَّل | الواقع في الكود |
|---|---|
| «ملف CI بانتظار الإضافة يدويًا» | **موجود فعلًا**: `.github/workflows/ci.yml` منذ ١ أغسطس. البند يُغلق بلا عمل — يحتاج فقط التأكد أنه يعمل على GitHub |
| «شاشة العروض (Proposals) غير موجودة — النسخ والإرسال عبر الـAPI فقط» | صحيح، لكن الـAPI **مكتمل**: `GET/POST /opportunities/:id/proposals`، `POST /proposals/:id/versions`، والإرسال — كلها في `approvals.controller.ts`. الناقص واجهة فقط، لا خلفية |
| «علاقات الشركات وفريق الفرصة: جدولان بلا API» | `AccountRelationship` و`OpportunityTeam` و`ContractClause` — ثلاثتها بلا API. البند الثالث مذكور منفصلًا وهو نفس النوع من العمل |

**أول إجراء:** تحديث `PROGRESS.md` بهذه الثلاثة قبل أي كود.

---

## ١. المبدأ الذي رُتّبت عليه الخطة

الترتيب ليس حسب الحجم ولا حسب سهولة التنفيذ، بل حسب **ما يمنع الإطلاق**:

1. **ما يمنع الإطلاق أمنيًا** — لا يُطلق نظام يحمل هوامش ربح وتكاليف موردين بلا TLS ولا طبقة إخفاء حقول.
2. **ما يمنع إتمام دورة العمل** — شاشة ناقصة في منتصف الدورة تعني أن المستخدم يخرج من النظام ليكمل عمله.
3. **ما يجعل النظام يبدو تقيلًا** — لا علاقة له بالوظائف، لكنه أول انطباع لكل مستخدم، وإصلاحه أرخص بكثير من قيمته.
4. **الثغرات داخل ما أُنجز** — الترتيب بينها حسب الأثر التشغيلي.
5. **الإصدارات الموقوفة على قرار من Afro** — لا تُبنى، بل يُلاحَق قرارها.

---

## المرحلة الأولى — ما يمنع الإطلاق (إلزامية)

لا إطلاق قبل اكتمال هذه المرحلة بالكامل. لا استثناءات.

### ١.١ تفعيل TLS

**الحالة:** النظام كله على HTTP داخل شبكة Tailscale خاصة. الكوكي التي تحمل التوكن تُرسل بلا `Secure` — الكود يقرأ `COOKIE_SECURE` من البيئة في `apps/web/src/app/api/auth/login/route.ts:31` والقيمة اليوم ليست `true`.

**لماذا "شبكة خاصة" ليست عذرًا:** Tailscale يحمي الطريق بين الأجهزة، لا بين الحاويات على السيرفر نفسه، ولا يحمي من جهاز مخترق داخل الشبكة. والأهم: اللحظة التي يُطلب فيها وصول من خارج الشبكة — وهي آتية مع قرار البوابات — ستجد النظام غير مستعد.

**العمل:**
1. إضافة reverse proxy (Caddy أو nginx) أمام `web` و`api` في `docker/docker-compose.prod.yml`. Caddy أبسط لأنه يدير الشهادة تلقائيًا.
2. ضبط `COOKIE_SECURE=true` في البيئة.
3. تحويل `web` و`api` إلى الاستماع على الشبكة الداخلية فقط، والـproxy وحده هو المنشور — نفس المبدأ المطبَّق بالفعل على Postgres وMinIO، وهو مبدأ صحيح يستحق التعميم.
4. إضافة فحص في `smoke.sh` يرفض أي رد يأتي على HTTP، وفحص يتأكد أن الكوكي تحمل `Secure` و`HttpOnly`.

**معيار الإنجاز:** `smoke.sh` يمر على HTTPS، ومحاولة الوصول على HTTP تُرفض أو تُحوَّل.

### ١.٢ ABAC وإخفاء الحقول الحساسة

**الحالة:** مؤجل. البنية مصمَّمة لاستيعابه، والتنفيذ لم يبدأ.

**لماذا هو مانع للإطلاق لا تحسينًا:** النظام يحوي هوامش الربح، تكاليف الموردين، أسعار عروضهم، وحدود الاعتماد. اليوم من يملك دورًا يقرأ الفرصة يقرأ كل ما فيها. هذا ليس نقصًا في ميزة — هو تسريب معلومات تجارية بين موظفين ليس لهم بها علاقة، ومورّد له حساب سيرى أسعار منافسيه.

**العمل:**
1. **تعريف مصفوفة الحقول الحساسة قبل كتابة أي كود** — أي حقل، أي دور يراه، وأي دور يراه مخفيًا أو مقنّعًا. هذه المصفوفة قرار من Afro لا من الكود، لكنها صغيرة ويمكن اقتراحها ومراجعتها في جلسة واحدة.
2. تنفيذ الطبقة عند **الاستجابة في الـAPI** لا في الواجهة. الإخفاء في الواجهة ليس إخفاءً — البيانات تكون قد وصلت المتصفح بالفعل.
3. تطبيقها على: `CostBreakdown`، `PartnerQuotation` وبنودها، `CostingScenario`، هوامش `Opportunity`، وحدود `ApprovalPolicy`.
4. اختبارات وحدة لكل دور × كل مورد حساس، وفحوص في `smoke.sh` تثبت أن دورًا محدودًا **لا** يرى الحقل.

**معيار الإنجاز:** فحص على السيرفر يسجّل الدخول بدور محدود ويثبت غياب الحقل من الرد، لا إخفاءه في الشاشة.

**ملاحظة على التقدير:** هذا أكبر بند في المرحلة الأولى، والتقدير الصادق أنه أسبوع عمل على الأقل، لأنه يمس ٢٠ وحدة API. تقسيمه على مرحلتين (الحقول المالية أولًا، ثم الباقي) خيار مقبول بشرط أن تكون الحقول المالية جاهزة قبل الإطلاق.

### ١.٣ التحقق من CI

**الحالة:** الملف موجود ولم يُتحقق من تشغيله.

**العمل:** دفعة تجريبية على فرع، والتأكد أن الاختبارات الـ٣٥٦ واللينت يعملان في البيئة السحابية لا محليًا فقط. إن كان يعمل، البند يُغلق ويُصحَّح في `PROGRESS.md`.

---

## المرحلة الثانية — إغلاق دورة العمل

### ٢.١ شاشة العروض (Proposals)

**الحالة:** الـAPI مكتمل، الواجهة معدومة. هذه أرخص فجوة في المشروع كله وأعلاها أثرًا.

**العمل:** شاشة تحت `app/[locale]/(app)/opportunities/[id]/proposals/` تعرض قائمة العروض ونسخها، وتتيح إنشاء نسخة والإرسال. النمط موجود ومكرر بالفعل في `QuotationComparison.tsx` و`CostingBuilder.tsx` — الشاشة تُبنى على نفس القالب لا من الصفر.

**لماذا أولًا:** لأن العرض هو مخرج الدورة. نظام يبني النطاق والتكلفة والاعتماد ثم يعجز عن إخراج العرض من واجهته يجبر المستخدم على مغادرته في آخر خطوة — وهي أسوأ نقطة يمكن أن يُترك عندها.

### ٢.٢ تحرير الـWorkflow وقواعده من الواجهة

**الحالة:** خطوات `WorkflowStep` وقواعد `ApprovalRule` تُبذر وتُعدَّل عبر قاعدة البيانات مباشرة. شاشة الإعدادات تحرّر **الحدود** فقط.

**لماذا هو مانع تشغيلي:** أي تغيير في دورة الاعتماد — وهو تغيير إداري عادي — يستدعي مطورًا وصولًا لقاعدة الإنتاج. وتعدد الدول (السؤال رقم ٤ المفتوح) يعني تعدد دورات اعتماد، أي أن هذه الفجوة ستتضاعف لا تثبت.

**العمل:** توسيع `ApprovalPolicySettings.tsx` ليحرّر الخطوات والقواعد لا الحدود وحدها، مع API مقابل. كل تعديل يُسجَّل في `AuditLog` — تغيير قاعدة اعتماد قرار حوكمة ويجب أن يُعرف من غيّره.

### ٢.٣ العروض المنتهية تسقط بصمت

**الحالة:** العرض المنتهي يسقط من المقارنة بلا إشعار لأحد.

**لماذا يستحق موضعًا مبكرًا رغم صغره:** الفشل هنا صامت. المستخدم يفتح المقارنة فيجد عرضًا ناقصًا ولا يعرف أنه كان موجودًا. الأخطاء الصامتة أغلى من الأخطاء الصاخبة دائمًا.

**العمل:** قاعدة في `NotificationRule` تُشعر قبل الانتهاء بمدة قابلة للضبط، ووسم بصري في `QuotationComparison.tsx` للعرض المنتهي بدل إسقاطه.

---

## المرحلة الثالثة — الأداء (الإحساس بالثقل)

هذه المرحلة لا تضيف وظيفة واحدة، ومع ذلك هي أعلى بند في الخطة من حيث نسبة الأثر إلى الجهد. والسبب الجذري ليس ما يُتوقع عادةً: **الواجهة نظيفة جدًا** — Next 15 وReact 19 وnext-intl فقط، بلا أي مكتبة تقيلة، وبلا أي `setInterval` في المشروع كله. المشكلة في نمط جلب البيانات لا في حجم الكود.

### ٣.١ حالات التحميل — السبب الأكبر

**التشخيص:** لا يوجد `loading.tsx` ولا `Suspense` في **أي مكان** في المشروع. كل صفحة server component تنتظر بياناتها كاملة قبل رسم أي شيء. النتيجة أن الشاشة القديمة تبقى مجمّدة بعد كل ضغطة حتى يعود الطلب. الصفحة ليست بطيئة بالضرورة — المستخدم فقط لا يرى أن شيئًا يحدث، وهذا هو تعريف «تقيل» عمليًا.

**العمل:** `loading.tsx` لكل مسار في `app/[locale]/(app)/`، بهيكل عظمي يطابق شكل الصفحة لا بدوّارة عامة. و`useTransition` على روابط التنقل في `Sidebar.tsx` — النمط مستخدم بالفعل ومطبَّق بشكل صحيح في `ListFilters.tsx:44`، فهو تعميم لشيء قائم لا اختراع جديد.

**هذا البند وحده يغيّر الانطباع بالكامل ولا يمس أي منطق.** يُنفَّذ أولًا.

### ٣.٢ `cache: 'no-store'` على كل طلب

**التشخيص:** `apps/web/src/lib/api.ts:27` يفرض `no-store` على كل نداء بلا استثناء. كل تنقّلة ترجع للـAPI وللداتابيز من الصفر.

**العمل:** التفرقة بين بيانات تتغيّر بالثانية وبيانات لا تتغيّر عمليًا. القوائم المرجعية (`RefList`، `RefListItem`)، وقوائم المستخدمين، وتعريفات المؤشرات — كلها تُقرأ في كل صفحة تقريبًا وتتغيّر نادرًا. `revalidate` قصير عليها مكسب مجاني. بيانات الفرص والاعتمادات تبقى `no-store` — التخزين المؤقت هنا خطأ لا تحسين.

**تحذير:** لا يُعمَّم التخزين المؤقت على أي شيء يمر بطبقة ABAC، وإلا خُزّن رد دور واسع وقُدّم لدور محدود. هذا سبب كافٍ لتنفيذ ١.٢ قبل ٣.٢.

### ٣.٣ الضغط على الـAPI

**التشخيص:** `helmet` موجود في `main.ts:20` لكن لا يوجد `compression`. ردود JSON لصفحة الفرصة كبيرة وتُرسل خامًا عبر Tailscale.

**العمل:** إضافة `compression` كـmiddleware. سطر واحد، وأثره على شبكة بعيدة ملموس.

### ٣.٤ صفحة الفرصة — سبعة طلبات

**التشخيص:** `opportunities/[id]/page.tsx:135` يجلب سبعة موارد. وهي **متوازية بـ`Promise.all` وهذا صحيح** — لكن الصفحة تنتظر أبطأها.

**العمل:** بعد تنفيذ ٣.١، تُلَف الأقسام الثانوية (الأنشطة، المستندات) في `Suspense` منفصل حتى يظهر رأس الفرصة فورًا ويصل الباقي بعده. لا داعي لدمج الطلبات في نقطة نهاية واحدة — ذلك يعقّد الـAPI مقابل مكسب أقل.

### ٣.٥ قياس قبل وبعد

**لا يُقبل هذا الملف بلا قياس.** الإحساس بالبطء ليس معيارًا. قبل البدء تُسجَّل أزمنة تحميل الشاشات الخمس الأكثر استخدامًا من السيرفر لا من جهاز محلي، وتُقارن بعد كل بند. بلا هذا لن يُعرف أي بند نفع وأيها كان جهدًا ضائعًا.

---

## المرحلة الرابعة — الثغرات داخل ما أُنجز

مرتّبة حسب الأثر التشغيلي:

| # | البند | العمل | ملاحظة |
|---|---|---|---|
| ٤.١ | **علاقات الشركات، فريق الفرصة، بنود العقد** | ثلاثة نماذج بلا API: `AccountRelationship`، `OpportunityTeam`، `ContractClause`. تُبنى بنفس نمط الوحدات العشرين القائمة، ثم شاشاتها | عمل متكرر ومعروف الشكل — مرشّح جيد للتنفيذ المتوازي |
| ٤.٢ | **تحويل عميل محتمل بلا شركة معروفة** | إتاحة إنشاء عميل جديد من داخل شاشة التحويل | احتكاك يومي حقيقي: اليوم يجب فتح ملف العميل أولًا ثم العودة |
| ٤.٣ | **التحرير المباشر في شبكة التكلفة** | تحرير داخل الخلية بدل نموذج لكل سطر في `CostingBuilder.tsx` | **البند الأعلى تكلفة في هذه المرحلة.** الشاشة تعمل اليوم — هذا تحسين تجربة لا إصلاح عطل، ويُؤجَّل بلا تردد إذا ضاق الوقت |
| ٤.٤ | **جدول الإنتاجية (Productivity Rate)** | جدول ومكتبة حسب الدولة والتضاريس بدل الإدخال اليدوي | يحتاج بيانات من Afro لا كودًا فقط — يُطلب مبكرًا |
| ٤.٥ | **قواعد الضرائب (بند ١٩)** | نفس بنية `CostRule` تمامًا، وباعتماد مالي مثلها | بند مستقل قابل للتنفيذ بلا انتظار أحد |
| ٤.٦ | **مدة السداد وقيمة الخصم** | `PAYMENT_TERM_DAYS` و`DISCOUNT_PERCENT` تُبلَّغان «غير محسومة» لغياب مصدر الرقم في الفرصة | **ليس عيبًا برمجيًا** — يحتاج قرارًا: من أين يأتي الرقم؟ |
| ٤.٧ | **Create from Template والسحب والإفلات في النطاق** | الاستيراد من Excel أُنجز؛ الباقي القوالب والسحب | تحسين إدخال لا وظيفة ناقصة |
| ٤.٨ | **تقارير ديناميكية فوق طبقة المؤشرات** | التعريفات مركزية بالفعل، فبناء تقرير يختار مؤشرات ونطاقًا عمل صغير | مكسب كبير مقابل جهد صغير — يُقدَّم على ٤.٣ و٤.٧ |
| ٤.٩ | **`Solution Strategy`** | **لا عمل.** بند معلن في فهرس الإصدار الثالث بلا جدول ولا شرح في أي موضع | غير قابل للبناء بلا توضيح — يُنقل إلى قائمة الأسئلة لا قائمة العمل |

---

## المرحلة الخامسة — الإصدارات الموقوفة على قرار Afro

هذه **لا تُخطَّط بتاريخ**، لأن تاريخها ليس بيدنا. ما يُخطَّط هو ملاحقة القرار:

| الإصدار | موقوف على | الوضع |
|---|---|---|
| ٨ و٩ — Nama ERP والعمليات | هل توجد واجهات برمجية أصلًا؟ (سؤال ٧) | `docs/integration-request.md` جاهز ومفصّل بأسئلته السبع — **يحتاج متابعة لا كتابة** |
| ١٠ — الرقابة التجارية | سؤال واحد محدد: هل يربط الـERP بند مصروف بأمر شراء أو كود فرصة؟ (سؤال ٦ في المستند) | **كل ما يلزم من جانبنا جاهز**: خط أساس التكلفة مثبَّت، كود التكلفة محفوظ على الترسية، الأعباء محسوبة، وستة مؤشرات تنتظر رقم التكلفة الفعلية |
| ١١ — الذكاء الاصطناعي | سحابي أم داخلي؟ (سؤال ٦) | وحدة `ai` مبنية بالفعل: `ai-router.service.ts` وطبقة `providers` — **البنية تستوعب الخيارين**، والقرار يحدد المزوّد لا المعمار |
| ١٢ — البوابات الخارجية | قرار: مستخدمون من خارج الشركة؟ | اللوحات منجزة. البوابات مصنَّفة **مستقبلية بنصّ الوثيقة** |
| — | عدد الكيانات القانونية والدول (سؤال ٤) | يؤثر مباشرة على ٢.٢ (تعدد دورات الاعتماد) — **يُسأل قبل بدء ٢.٢ لا بعده** |

**الإجراء الوحيد المطلوب هنا:** رسالة واحدة إلى Afro تحمل الأسئلة الأربعة المتبقية مرتّبة حسب ما يعطّله كل سؤال. السؤال رقم ٤ أعلاها إلحاحًا لأنه يعطّل عملًا مجدولًا في المرحلة الثانية.

---

## ٦. الترتيب المقترح للتنفيذ

**المسار الحرج نحو الإطلاق:**

```
١.١ TLS  ──┐
١.٣ CI    ─┼──► ١.٢ ABAC ──► ٣.٢ التخزين المؤقت
٣.١ التحميل┘         │
                     ▼
              ٢.١ شاشة العروض ──► ٢.٣ إشعار الانتهاء
                     │
                     ▼
              ٢.٢ تحرير الـWorkflow  ◄── يحتاج جواب السؤال ٤
```

**ما يمكن تنفيذه متوازيًا بلا انتظار أحد:** ٣.١ و٣.٣ و٤.١ و٤.٥ و٤.٨.

**التسلسل الموصى به:**

1. **أولًا وفورًا:** ٣.١ (حالات التحميل) و٣.٣ (الضغط). لا يمسّان منطقًا، وأثرهما على الانطباع فوري، وينجزان في يوم.
2. **بالتوازي:** إرسال الأسئلة الأربعة إلى Afro. الانتظار يبدأ من اليوم لا من حين نفرغ.
3. **ثم:** ١.١ و١.٣ — بنيتان تحتيتان مستقلتان.
4. **ثم:** ١.٢ ABAC — أكبر بند وأهمه، ولا إطلاق قبله.
5. **ثم:** ٣.٢ التخزين المؤقت — **بعد ABAC حصرًا** للسبب المذكور في ٣.٢.
6. **ثم:** ٢.١ فـ٢.٣ فـ٢.٢.
7. **ثم:** المرحلة الرابعة بترتيبها، مع تأجيل ٤.٣ و٤.٧ بلا حرج.

---

## ٧. ما لا تفعله هذه الخطة — بوضوح

- **لا تضع تواريخ للإصدارات ٨ و٩ و١٠ و١١.** تواريخها بيد Afro. وضع تاريخ لعمل موقوف على جواب من طرف آخر هو وعد لا خطة.
- **لا تعالج `Solution Strategy`.** غير قابل للبناء بلا توضيح، ووضعه في قائمة العمل يوهم بأنه مجدول.
- **لا تعد بأن ٤.٣ سيُنجز.** التحرير داخل الخلية أغلى بند مقابل شاشة تعمل اليوم، وهو أول ما يسقط عند ضيق الوقت.

## ٨. معيار الجاهزية للإطلاق

النظام جاهز للإنتاج حين، ولا يكون جاهزًا قبل أن:

1. تكتمل المرحلة الأولى بالكامل — TLS وABAC وCI متحقق منه.
2. تكتمل ٢.١ — دورة العمل تُغلق من داخل الواجهة.
3. يمر `smoke.sh` بفحوصه الـ٢٨٠ **على HTTPS**، مضافًا إليها فحوص ABAC الجديدة.
4. تُقاس أزمنة الشاشات الخمس الأكثر استخدامًا من السيرفر وتُقارن بخط الأساس المسجَّل قبل المرحلة الثالثة.

ما عدا ذلك — المراحل الرابعة والخامسة — يمكن أن يجري والنظام في الإنتاج.

---

## ٩. تحديث هذا الملف

نفس قاعدة `PROGRESS.md`: بعد كل بند يكتمل ويُثبَت على السيرفر، يُنقل من هنا إلى `§٢` في `PROGRESS.md` بوصف ما صار ممكنًا فعليًا. وأي ثغرة جديدة تظهر أثناء التنفيذ تُسجَّل فورًا — الثغرة المعروفة أرخص من المفاجأة.
