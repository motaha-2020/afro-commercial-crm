# تاسكات ACMS — من الخطة إلى التنفيذ

> مشتقّ من [`PLAN.md`](PLAN.md) · وُضع **٢٠٢٦-٠٨-١٥**
> كل تاسك له: سبب، تبعية، معيار إنجاز، وبرومت جاهز للّصق.
> **قاعدة ثابتة تنطبق على كل تاسك بلا استثناء:** لا يُغلق تاسك قبل إثباته على السيرفر عبر `scripts/smoke.sh` — لا على جهاز محلي.

---

## جدول التاسكات

| # | التاسك | المرحلة | يعتمد على | الحجم |
|---|---|---|---|---|
| T-00 | تصحيح `PROGRESS.md` | ٠ | — | ساعة |
| T-01 | تفعيل TLS | ١ مانع | — | يوم |
| T-02 | مصفوفة الحقول الحساسة | ١ مانع | قرار Afro | يوم |
| T-03 | تنفيذ ABAC | ١ مانع | T-02 | **أسبوع+** |
| T-04 | التحقق من CI | ١ مانع | — | ساعة |
| T-05 | شاشة العروض | ٢ | — | يومان |
| T-06 | تحرير الـWorkflow | ٢ | جواب سؤال ٤ | ٣ أيام |
| T-07 | إشعار انتهاء العروض | ٢ | — | يوم |
| T-08 | قياس خط الأساس | ٣ | — | ساعتان |
| T-09 | حالات التحميل | ٣ | T-08 | يوم |
| T-10 | الضغط على الـAPI | ٣ | T-08 | ساعة |
| T-11 | التخزين المؤقت الانتقائي | ٣ | **T-03** | يوم |
| T-12 | Suspense لصفحة الفرصة | ٣ | T-09 | نصف يوم |
| T-13 | ثلاث واجهات برمجية ناقصة | ٤ | — | ٣ أيام |
| T-14 | تحويل عميل محتمل بلا شركة | ٤ | — | نصف يوم |
| T-15 | قواعد الضرائب | ٤ | — | ٣ أيام |
| T-16 | تقارير ديناميكية | ٤ | — | يومان |
| T-17 | جدول الإنتاجية | ٤ | بيانات Afro | يومان |
| T-18 | التحرير داخل خلية التكلفة | ٤ | T-09 | **٤ أيام — قابل للإسقاط** |
| T-19 | قوالب النطاق والسحب والإفلات | ٤ | — | يومان — قابل للإسقاط |
| **T-20** | **توصيل وحدة الـAI الميتة** | **٥ AI** | — | يوم |
| **T-21** | **واجهة برمجية للمحادثة** | **٥ AI** | T-20 | يومان |
| **T-22** | **شاشة مراجعة الاقتراحات** | **٥ AI** | T-21 | يومان |
| **T-23** | **حاجز الخصوصية للمستندات** | **٥ AI** | T-03، قرار Afro | ٣ أيام |
| **T-24** | **أول وكيل فعلي: فحص التكلفة** | **٥ AI** | T-22 | ٣ أيام |
| **T-25** | **تدقيق وحوكمة الـAI** | **٥ AI** | T-21 | يومان |
| T-26 | الأسئلة الأربعة إلى Afro | ٦ | — | ساعة |

---

## المرحلة ٠ — تصحيح السجل

### T-00 · تصحيح `PROGRESS.md`

**لماذا:** فحص الكود كشف ثلاث نقاط مسجَّلة خطأً. خطة مبنية على حالة خاطئة تهدر وقتًا.
**معيار الإنجاز:** الملف يطابق الكود في النقاط الثلاث.

```
افتح PROGRESS.md وصحّح ثلاث نقاط في §٣، كل واحدة تخالف ما في الكود:

١. البند «ملف CI — بانتظار الإضافة يدويًا»: الملف موجود فعلًا في
   .github/workflows/ci.yml منذ ١ أغسطس. احذف البند من §٣ وانقله إلى §٢
   بوصف ما صار ممكنًا، بشرط أن يكون T-04 قد أثبت أنه يعمل. إن لم يُثبت بعد،
   غيّر الوصف إلى «موجود، لم يُتحقق من تشغيله».

٢. البند «شاشة العروض (Proposals) غير موجودة»: صحيح للواجهة فقط. الـAPI
   مكتمل — GET/POST /opportunities/:id/proposals و POST /proposals/:id/versions
   والإرسال، كلها في apps/api/src/approvals/approvals.controller.ts سطر ١٢٥
   وما بعده. عدّل الوصف ليقول إن الناقص واجهة فقط لا خلفية، لأن هذا يغيّر
   تقدير حجم العمل جذريًا.

٣. البند «علاقات الشركات وفريق الفرصة — جدولان بلا API»: هي ثلاثة لا اثنان.
   AccountRelationship و OpportunityTeam و ContractClause. بند العقد مذكور
   منفصلًا في §٣ وهو نفس نوع العمل تمامًا — ادمج الثلاثة في بند واحد.

أضف أيضًا بندًا جديدًا في §٣ لم يكن مسجّلًا وكشفه الفحص:
«وحدة الـAI: خدمتان مكتوبتان وغير موصولتين» — AiMemoryService و
AiSuggestionService موجودتان كملفين في apps/api/src/ai/ لكنهما غير مسجّلتين
في ai.module.ts وغير مستدعاتين من أي موضع في المشروع. ولا يوجد أي
@Controller في الوحدة، أي صفر نقاط نهاية.

لا تكتب كودًا في هذا التاسك. سجّل الواقع فقط، وحدّث التاريخ في أعلى الملف.
```

---

## المرحلة ١ — ما يمنع الإطلاق

### T-01 · تفعيل TLS

**لماذا:** النظام كله على HTTP. الكوكي الحاملة للتوكن بلا `Secure`. Tailscale يحمي الطريق بين الأجهزة لا بين الحاويات، ولا يحمي من جهاز مخترق داخل الشبكة.
**معيار الإنجاز:** `smoke.sh` يمر على HTTPS، وHTTP يُرفض أو يُحوَّل.

```
فعّل TLS على نشر ACMS. الوضع الحالي: كل شيء على HTTP داخل شبكة Tailscale
خاصة، والكوكي الحاملة للتوكن تُرسل بلا Secure — الكود يقرأ COOKIE_SECURE من
البيئة في apps/web/src/app/api/auth/login/route.ts:31 والقيمة اليوم ليست true.

اعمل الآتي:

١. أضف Caddy كـreverse proxy في docker/docker-compose.prod.yml أمام web و api.
   Caddy لا nginx، لأنه يدير الشهادة وتجديدها تلقائيًا ويقلّل ما يجب صيانته.

٢. حوّل web و api إلى الاستماع على الشبكة الداخلية للـcompose فقط، بحيث يكون
   الـproxy هو المنشور وحده. هذا تعميم لمبدأ مطبَّق بالفعل وبشكل صحيح على
   Postgres و MinIO — راجع docker/docker-compose.prod.yml وقلّده، لا تخترع
   نمطًا جديدًا.

٣. اضبط COOKIE_SECURE=true في البيئة، وحدّث .env.example بتعليق يشرح متى
   تكون true ولماذا.

٤. أضف إلى scripts/smoke.sh فحصين:
   - طلب على HTTP يجب أن يُرفض أو يُحوَّل إلى HTTPS، لا أن يُخدَم.
   - الكوكي في رد تسجيل الدخول تحمل Secure و HttpOnly و SameSite.

انتبه: WEB_ORIGIN في apps/api/src/main.ts:29 يضبط CORS ويجب أن يتحدّث إلى
مصدر https وإلا انكسر تسجيل الدخول بعد التحويل. تفقّد كل موضع يبني رابطًا
مطلقًا بـhttp:// قبل أن تعلن الانتهاء.

أثبت على السيرفر لا محليًا. الشهادة على جهاز محلي لا تثبت شيئًا.
```

### T-02 · مصفوفة الحقول الحساسة

**لماذا:** ABAC بلا مصفوفة متفَق عليها هو تخمين. المصفوفة قرار من Afro لا من الكود، لكنها صغيرة ويمكن اقتراحها في جلسة واحدة.
**معيار الإنجاز:** مستند مراجَع ومعتمَد من Afro، لا كود.
**الحالة (٢٠٢٦-٠٨-١٨):** المسودة جاهزة في [`docs/field-visibility-matrix.md`](docs/field-visibility-matrix.md) — خمس مجموعات حقول مستخرجة من السكيما، جدول اثني عشر دورًا، وأربعة أسئلة مفتوحة. **بانتظار موافقة Afro على §٤ وحدها**؛ لا شيء آخر يعطّل T-03.

```
قبل كتابة أي سطر من ABAC، أنتج مستند docs/field-visibility-matrix.md يقترح
مصفوفة الحقول الحساسة على Afro Group لمراجعتها.

اقرأ prisma/schema.prisma واستخرج فعليًا — لا من الذاكرة — كل حقل يحمل قيمة
تجارية حساسة في النماذج: CostBreakdown، CostElement، PartnerQuotation
وPartnerQuotationItem، CostingScenario و CostingVersion، BoqItem، هوامش
Opportunity، وحدود ApprovalPolicy.

اقرأ apps/api/src/users و UserRole في السكيما لتعرف الأدوار الفعلية الموجودة
اليوم، ولا تفترض أدوارًا غير موجودة.

اجعل المستند جدولًا: الحقل، النموذج، أي دور يراه كاملًا، أي دور يراه مقنّعًا،
أي دور لا يراه إطلاقًا. ولكل صف سطر واحد يشرح الضرر لو رآه من لا يجب —
هذا السطر هو ما سيراجعه Afro فعليًا، لا أسماء الحقول.

اطرح في نهاية المستند الأسئلة التي لا يستطيع الكود حسمها. أهمها على الأرجح:
هل سيكون للموردين حسابات على النظام؟ لأن الإجابة بنعم تعني أن مورّدًا قد يرى
أسعار منافسيه، وهي أخطر حالة في المصفوفة كلها.

لا تكتب كودًا. مخرج هذا التاسك مستند فقط.
```

### T-03 · تنفيذ ABAC وإخفاء الحقول

**لماذا:** أكبر بند في الخطة وأهمه. اليوم من يقرأ الفرصة يقرأ كل ما فيها — هوامش الربح وتكاليف الموردين وحدود الاعتماد.
**يعتمد على:** T-02 · **يحجب:** T-11
**معيار الإنجاز:** فحص على السيرفر يسجّل الدخول بدور محدود ويثبت **غياب** الحقل من الرد.

```
نفّذ طبقة ABAC وإخفاء الحقول الحساسة في ACMS، بناءً على المصفوفة المعتمدة في
docs/field-visibility-matrix.md.

القاعدة المعمارية التي لا يجوز كسرها: الإخفاء يتم عند تشكيل الاستجابة في
الـAPI، لا في الواجهة. الإخفاء في الواجهة ليس إخفاءً — تكون البيانات قد وصلت
المتصفح بالفعل وظهرت في تبويب الشبكة.

اعمل الآتي:

١. ابنِ الطبقة كـinterceptor أو serializer مركزي في apps/api/src/common/،
   يقرأ دور المستخدم من الطلب ويطبّق المصفوفة على الرد الخارج. وحدة واحدة
   مركزية لا شرط متناثر في عشرين خدمة — وإلا استحال إثبات التغطية.

٢. طبّقها على: CostBreakdown، PartnerQuotation وبنودها، CostingScenario،
   هوامش Opportunity، وحدود ApprovalPolicy.

٣. اكتب اختبار وحدة لكل دور × كل مورد حساس. الاختبار يثبت الغياب لا القناع.

٤. أضف فحوصًا في scripts/smoke.sh تسجّل الدخول بدور محدود على السيرفر وتؤكد
   أن الحقل غير موجود في الرد أصلًا.

انتبه لثلاث مصائد:
- التصدير والاستيراد: apps/api/src/imports قد يسرّب ما أخفته الطبقة. تفقّده.
- المؤشرات: apps/api/src/metrics تحسب فوق بيانات حساسة، والمؤشر المجمَّع قد
  يكشف المفرد. راجعها بوعي.
- سجل التدقيق: AuditLog قد يخزّن قيمًا حساسة في حقول التغيير.

هذا البند يمسّ ٢٠ وحدة API وهو أسبوع عمل على الأقل. إن ضاق الوقت، قسّمه:
الحقول المالية أولًا وهي شرط الإطلاق، والباقي بعده. لا تقسّمه بأي شكل آخر.
```

### T-04 · التحقق من CI

**لماذا:** الملف موجود ولم يُتحقق من تشغيله. `PROGRESS.md` يظنه غير موجود.

```
تحقّق أن .github/workflows/ci.yml يعمل فعلًا على GitHub لا محليًا فقط.

ادفع فرعًا تجريبيًا وتأكد أن الاختبارات الـ٣٥٦ واللينت الموحّد يمران في
البيئة السحابية. الفشل المتوقع هنا هو اعتماد الاختبارات على قاعدة بيانات أو
على متغيرات بيئة غير متاحة في الـrunner — تفقّد هذا أولًا قبل أي شيء آخر.

إن مرّ: أغلق البند في PROGRESS.md كما هو موصوف في T-00.
إن فشل: أصلح الملف حتى يمر، ولا تعطّل الاختبارات لتجعله يمر.
```

---

## المرحلة ٢ — إغلاق دورة العمل

### T-05 · شاشة العروض

**لماذا:** أرخص فجوة في المشروع وأعلاها أثرًا — الـAPI مكتمل والواجهة معدومة. العرض هو مخرج الدورة، والمستخدم اليوم يُترك عند آخر خطوة.

```
ابنِ شاشة العروض (Proposals) في الواجهة. الـAPI مكتمل بالفعل — لا تبنِ
خلفية جديدة، وتحقّق من ذلك بنفسك أولًا في
apps/api/src/approvals/approvals.controller.ts من سطر ١٢٥:
  GET  /opportunities/:id/proposals
  POST /opportunities/:id/proposals
  POST /proposals/:id/versions
  والإرسال.

أنشئ المسار apps/web/src/app/[locale]/(app)/opportunities/[id]/proposals/
يعرض قائمة العروض وسجل نسخ كل عرض، ويتيح إنشاء نسخة جديدة وإرسال العرض.

لا تخترع نمطًا. القالب موجود ومكرر في المشروع — اقرأ
apps/web/src/components/QuotationComparison.tsx و CostingBuilder.tsx وقلّد
بنيتهما في تقسيم server component للجلب و client component للتفاعل.

الترجمات الثلاث إلزامية (عربي RTL وإنجليزي وفرنسي) — راجع كيف تفعلها الشاشات
القائمة عبر next-intl ولا تترك نصًا مكتوبًا مباشرة في الشاشة.

أضف رابط الشاشة إلى صفحة الفرصة وإلى Sidebar.tsx إن كان مناسبًا هناك.
وأضف فحوصًا في smoke.sh تثبت أن الشاشة تُحمّل وتعرض عرضًا موجودًا.
```

### T-06 · تحرير الـWorkflow وقواعده

**لماذا:** أي تغيير في دورة الاعتماد — وهو تغيير إداري عادي — يستدعي مطورًا وصولًا لقاعدة الإنتاج. وتعدد الدول يضاعف الفجوة لا يثبّتها.
**يعتمد على:** جواب السؤال ٤ من Afro.

```
وسّع تحرير الحوكمة من الواجهة ليشمل خطوات الـWorkflow وقواعد الاعتماد، لا
الحدود وحدها.

الوضع اليوم: WorkflowDefinition و WorkflowStep و ApprovalRule تُبذر وتُعدَّل
عبر قاعدة البيانات مباشرة. شاشة apps/web/src/components/ApprovalPolicySettings.tsx
تحرّر الحدود فقط.

قبل أن تبدأ، تأكد أن Afro أجابت السؤال رقم ٤ عن عدد الكيانات القانونية
والدول. الإجابة تغيّر شكل الشاشة نفسها: دولة واحدة تعني دورة اعتماد واحدة،
وعدة دول تعني أن الشاشة يجب أن تختار الدورة قبل تحريرها. لا تبنِ على افتراض.

اعمل الآتي:
١. نقاط نهاية لإدارة WorkflowDefinition و WorkflowStep و ApprovalRule، بنفس
   نمط الوحدات القائمة في apps/api/src/approvals/.
٢. وسّع ApprovalPolicySettings.tsx أو أضف شاشة مجاورة لتحرير الخطوات
   والقواعد.
٣. كل تعديل يُسجَّل في AuditLog باسم من غيّره وما كان وما صار. تغيير قاعدة
   اعتماد قرار حوكمة، ولا يجوز أن يمر بلا أثر.
٤. امنع الحالات التي تكسر الدورة: خطوة بلا معتمِد، أو دورة بلا خطوة نهائية،
   أو حذف خطوة عليها طلبات معلّقة. الرفض برسالة تشرح السبب لا بخطأ عام.
```

### T-07 · إشعار انتهاء العروض

**لماذا:** الفشل صامت. العرض المنتهي يسقط من المقارنة والمستخدم لا يعرف أنه كان موجودًا. الأخطاء الصامتة أغلى من الصاخبة دائمًا.

```
أصلح سقوط عروض الموردين المنتهية بصمت.

السلوك اليوم: العرض المنتهي صلاحيته يسقط من المقارنة بلا أي إشعار، فيفتح
المستخدم المقارنة ويجد عرضًا ناقصًا ولا يعرف أنه كان موجودًا.

اعمل شيئين:
١. قاعدة في NotificationRule تُشعر المسؤول قبل انتهاء العرض بمدة قابلة
   للضبط من شاشة الإعدادات لا مثبّتة في الكود — نفس المبدأ المطبَّق على حدود
   الاعتماد، فاقرأ كيف نُفّذ هناك واتبعه.
٢. في apps/web/src/components/QuotationComparison.tsx: أظهر العرض المنتهي
   بوسم بصري واضح بدل إسقاطه من الجدول. المستخدم يجب أن يرى أنه كان هنا
   وانتهى، لا أن يختفي.

انتبه أن العرض المنتهي يجب أن يبقى مستبعَدًا من الاختيار والحسابات — الظهور
للعلم لا للاستخدام.
```

---

## المرحلة ٣ — الأداء

### T-08 · قياس خط الأساس

**لماذا:** الإحساس بالبطء ليس معيارًا. بلا قياس لن يُعرف أي بند نفع وأيها كان جهدًا ضائعًا.

```
سجّل خط أساس لأداء ACMS قبل أي تحسين.

قِس من السيرفر لا من جهاز محلي — الفرق هو زمن Tailscale وهو جزء من المشكلة
لا ضجيج فيها.

قِس الشاشات الخمس الأكثر استخدامًا: لوحة المعلومات، قائمة الفرص، تفاصيل
الفرصة، قائمة العملاء، شاشة التكلفة.

لكل شاشة سجّل: زمن أول بايت، وزمن ظهور المحتوى، وعدد نداءات الـAPI وزمن
كل نداء على حدة. الرقم الأخير هو الأهم لأنه سيكشف أي نداء بعينه هو البطيء.

اكتب النتائج في docs/perf-baseline.md بصيغة تسمح بإعادة القياس ومقارنته
لاحقًا. سجّل مع الأرقام: تاريخ القياس، وحالة الشبكة، وحجم البيانات في
الجداول المعنية — رقم بلا سياقه لا يُقارن به.
```

### T-09 · حالات التحميل

**لماذا:** السبب الأكبر للإحساس بالثقل. لا يوجد `loading.tsx` ولا `Suspense` في أي مكان — الشاشة القديمة تتجمد بعد كل ضغطة. الصفحة ليست بطيئة بالضرورة، المستخدم فقط لا يرى أن شيئًا يحدث.

```
عالج السبب الأكبر للإحساس بأن ACMS تقيل: غياب حالات التحميل تمامًا.

التشخيص المؤكد بالفحص: لا يوجد ملف loading.tsx ولا استخدام واحد لـSuspense
في المشروع كله. كل صفحة server component تنتظر بياناتها كاملة قبل رسم أي
شيء، فتبقى الشاشة القديمة مجمّدة بعد كل ضغطة حتى يعود الطلب.

اعمل الآتي:
١. loading.tsx لكل مسار تحت apps/web/src/app/[locale]/(app)/. بهيكل عظمي
   يطابق شكل الصفحة الفعلي — جدول بصفوف رمادية لصفحة جدول، بطاقات لصفحة
   بطاقات. لا دوّارة عامة واحدة لكل الشاشات: الهيكل المطابق يجعل الانتقال
   يبدو فوريًا، والدوّارة تجعله يبدو انتظارًا.
٢. useTransition على روابط التنقل في apps/web/src/components/Sidebar.tsx مع
   مؤشر بصري أثناء الانتقال.

النمط مستخدم بالفعل وبشكل صحيح في ListFilters.tsx:44 — اقرأه وعمّمه، فهذا
تعميم لشيء قائم لا اختراع جديد.

لا تمسّ أي منطق ولا أي نداء API في هذا التاسك. غيّر ما يراه المستخدم فقط.
بعد الانتهاء أعد قياس شاشات docs/perf-baseline.md وسجّل الفرق.
```

### T-10 · الضغط على الـAPI

```
أضف ضغط الردود إلى واجهة ACMS البرمجية.

apps/api/src/main.ts يستخدم helmet في السطر ٢٠ لكن لا يوجد compression
إطلاقًا. ردود JSON لصفحة الفرصة كبيرة وتُرسل خامًا عبر Tailscale.

أضف compression كـmiddleware في main.ts. اضبط عتبة حجم معقولة حتى لا تُضغط
الردود الصغيرة بلا داعٍ.

قِس الفرق فعليًا على أثقل رد في النظام — الأرجح أنه رد صفحة الفرصة أو شاشة
التكلفة — وسجّل حجم الرد قبل وبعد في docs/perf-baseline.md.
```

### T-11 · التخزين المؤقت الانتقائي

**⚠️ يعتمد على T-03 حصرًا.** تخزين رد لدور واسع وتقديمه لدور محدود يخرق طبقة الإخفاء.

```
عالج cache: 'no-store' المفروض على كل نداء بلا استثناء.

توقّف: لا تبدأ هذا التاسك قبل اكتمال ABAC (T-03). تخزين رد لدور واسع مؤقتًا
وتقديمه لدور محدود يخرق طبقة الإخفاء بالكامل. هذه ليست ملاحظة جانبية بل شرط
تنفيذ.

الوضع: apps/web/src/lib/api.ts:27 يفرض no-store على كل نداء، فكل تنقّلة
ترجع للـAPI وللداتابيز من الصفر.

فرّق بين نوعين:
- ما لا يتغيّر عمليًا ويُقرأ في كل صفحة: القوائم المرجعية RefList و
  RefListItem، قوائم المستخدمين، تعريفات المؤشرات. هذه تأخذ revalidate قصيرًا
  وهو مكسب مجاني.
- ما يتغيّر بالثانية: الفرص، الاعتمادات، التكلفة، العروض. هذه تبقى no-store.
  التخزين المؤقت هنا خطأ لا تحسين.

اجعل الاختيار صريحًا في توقيع apiFetch لا ضمنيًا، بحيث يضطر كاتب أي نداء
جديد أن يقرر بوعي. الافتراضي عند عدم التحديد يبقى no-store — الخطأ الآمن
أفضل من الخطأ الصامت.

لا تخزّن أي شيء يمر بطبقة ABAC مطلقًا.
```

### T-12 · Suspense لصفحة الفرصة

```
حسّن زمن ظهور صفحة تفاصيل الفرصة.

الوضع: apps/web/src/app/[locale]/(app)/opportunities/[id]/page.tsx:135 يجلب
سبعة موارد. وهي متوازية بـPromise.all وهذا صحيح ولا يجب تغييره — لكن الصفحة
تنتظر أبطأها قبل رسم أي شيء.

لُفّ الأقسام الثانوية — الأنشطة والمستندات — في حدود Suspense منفصلة، بحيث
يظهر رأس الفرصة وبياناتها الأساسية فورًا ويصل الباقي بعده.

لا تدمج النداءات في نقطة نهاية واحدة. ذلك يعقّد الـAPI مقابل مكسب أقل، ويخلق
نقطة نهاية خاصة بشاشة واحدة وهي أسوأ نوع من الاقتران.

قِس زمن ظهور المحتوى قبل وبعد وسجّله في docs/perf-baseline.md.
```

---

## المرحلة ٤ — ثغرات داخل ما أُنجز

### T-13 · ثلاث واجهات برمجية ناقصة

```
ابنِ الواجهات البرمجية الناقصة لثلاثة نماذج موجودة في قاعدة البيانات بلا أي
واجهة: AccountRelationship و OpportunityTeam و ContractClause.

كلها مخزَّنة اليوم ولا تُدار من أي مكان — تظهر في القراءة فقط، وبند العقد
لا يُدار إطلاقًا.

اقرأ وحدة قائمة ومكتملة أولًا — apps/api/src/contacts مثال جيد لصغره
ووضوحه — وقلّد نمطها بالكامل: الوحدة، الخدمة، المتحكم، DTO، التحقق، سجل
التدقيق، والاختبارات. لا تخترع نمطًا مختلفًا لأن هذا عمل متكرر معروف الشكل.

ثم شاشات الإدارة لكل منها، مع التعديل والحذف من الواجهة — راجع كيف نُفّذ
ذلك في PartnerRowActions.tsx و LeadRowActions.tsx واتبعه.

ثلاثتها مستقلة عن بعضها تمامًا، فهي مرشّح جيد للتنفيذ المتوازي إن توفّر أكثر
من منفّذ.

الترجمات الثلاث إلزامية. وفحوص smoke.sh لكل نقطة نهاية جديدة.
```

### T-14 · تحويل عميل محتمل بلا شركة معروفة

```
أزل احتكاكًا يوميًا في تحويل العميل المحتمل.

اليوم: التحويل يتطلب اختيار عميل موجود، فإن كان العميل جديدًا وجب على
المستخدم مغادرة الشاشة، وفتح شاشة العملاء، وإنشاء العميل، ثم العودة والبدء
من جديد.

أتح إنشاء عميل جديد من داخل شاشة التحويل نفسها. راجع
apps/web/src/components/LeadActions.tsx و NewAccountForm.tsx — النموذج
موجود ويحتاج استدعاءً في سياق التحويل لا إعادة كتابة.

انتبه أن العملية يجب أن تكون ذرّية: إنشاء العميل ثم فشل التحويل يجب ألا
يترك عميلًا يتيمًا بلا سبب وجود. راجع كيف تعالج الخدمات القائمة هذا في
apps/api/src/leads.
```

### T-15 · قواعد الضرائب

```
نفّذ قواعد الضرائب (بند ١٩ في وثيقة المشروع).

البنية جاهزة ومطابقة: هذا نفس نمط CostRule تمامًا — قواعد معتمَدة ماليًا
تُطبَّق على التكلفة. اقرأ apps/api/src/costing و نموذج CostRule في السكيما
و شاشة apps/web/src/components/CostRuleSettings.tsx، وابنِ نظيرها للضرائب.

يشمل ذلك: النموذج في السكيما مع الفهارس، الواجهة البرمجية، اعتماد مالي
للقاعدة مثل نظيرتها، شاشة الإدارة، وتطبيق القاعدة في حساب التكلفة.

انتبه لنقطة تصميمية: الضريبة تختلف بالدولة، والسؤال رقم ٤ عن عدد الدول لم
يُجَب بعد. صمّم النموذج ليحمل نطاقًا (دولة/كيان) من البداية حتى لو كانت
القيمة واحدة اليوم — إضافة النطاق لاحقًا هجرة مؤلمة على بيانات حية.
```

### T-16 · تقارير ديناميكية

```
ابنِ طبقة تقارير ديناميكية فوق طبقة المؤشرات.

هذا مكسب كبير مقابل جهد صغير، والسبب أن العمل التحضيري تمّ: تعريفات المؤشرات
صارت مركزية في apps/api/src/metrics — تعريف واحد لكل مؤشر. اقرأها أولًا
لتعرف ما المتاح فعلًا قبل أن تصمم شيئًا.

المطلوب: أن يختار المستخدم مؤشرات ونطاقًا زمنيًا وتصفية، فيحصل على تقرير.
مع حفظ التقرير لإعادة تشغيله.

قيدان لا تتجاوزهما:
- التقرير يقرأ من طبقة المؤشرات المركزية فقط. أي حساب جديد داخل التقرير
  يعني تعريفًا ثانيًا للمؤشر، وهو بالضبط ما عالجه توحيد التعريفات.
- التقرير يمر بطبقة ABAC مثل أي قراءة أخرى. تقرير مجمَّع قد يكشف قيمة مفردة
  محجوبة — انتبه لهذا تحديدًا عند التجميع على عدد صغير من السجلات.
```

### T-17 · جدول الإنتاجية

```
نفّذ مكتبة معدلات الإنتاجية (Productivity Rate).

اليوم تُدخل الإنتاجية يدويًا في كل سطر تكلفة، بدل مكتبة تُقرأ حسب الدولة
والتضاريس.

هذا التاسك يحتاج بيانات من Afro لا كودًا فقط. اطلبها مبكرًا وبشكل محدد: ما
معدلات الإنتاجية المعتمدة لديهم، مصنّفة بأي أبعاد بالضبط (الدولة؟ التضاريس؟
نوع النشاط؟ الموسم؟)، وبأي وحدة قياس. أرسل الطلب أولًا ثم ابنِ الهيكل بينما
تنتظر.

ابنِ النموذج والواجهة البرمجية وشاشة إدارة المكتبة، ثم اربطها بسطر التكلفة
بحيث يُقترح المعدل تلقائيًا مع إبقاء التجاوز اليدوي ممكنًا — المكتبة اقتراح
لا قيد، وسطر واحد شاذ لا يجب أن يعطّل المستخدم.
```

### T-18 · التحرير داخل خلية التكلفة ⚠️ قابل للإسقاط

```
حوّل شاشة بناء التكلفة إلى تحرير داخل الخلية بأسلوب جدول البيانات.

قبل أن تبدأ اعرف موقع هذا التاسك: الشاشة تعمل اليوم بنماذج لكل سطر. هذا
تحسين تجربة لا إصلاح عطل، وهو أغلى بند في مرحلته، وأول ما يسقط عند ضيق
الوقت. لا تبدأه قبل اكتمال كل ما هو مانع للإطلاق.

الشاشة: apps/web/src/components/CostingBuilder.tsx — موجودة بشبكة البنود
وتفصيل التكلفة القابل للفتح والإجماليات والتنبيهات البصرية. الناقص هو
التحرير داخل الخلية مباشرة.

المطلوب: تحرير في الخلية، تنقّل بالكيبورد بين الخلايا، حفظ عند مغادرة
الخلية، ولصق من الحافظة إن أمكن.

انتبه لثلاثة:
- الحفظ عند كل خلية يعني نداءات كثيرة. اجمعها أو أخّرها، ولا تفقد تعديلًا
  عند انقطاع الشبكة.
- الأخطاء يجب أن تظهر عند الخلية نفسها لا في رسالة عامة أعلى الشاشة.
- قفل الاعتماد قائم على السيناريو — تأكد أن الشبكة تحترمه ولا تسمح بتحرير
  نسخة مقفلة.
```

### T-19 · قوالب النطاق والسحب والإفلات ⚠️ قابل للإسقاط

```
أكمل ما تبقّى من ميزات بناء النطاق: القوالب والسحب والإفلات.

الاستيراد من Excel أُنجز بالفعل عبر الإطار الجملي ويشمل حزم النطاق وبنودها —
تحقّق من ذلك في apps/api/src/imports قبل أن تبني شيئًا، ولا تكرره.

الباقي شيئان:
١. Create from Template: حفظ نطاق كقالب، وإنشاء نطاق جديد من قالب. يحتاج
   نموذجًا وواجهة برمجية جديدين.
٢. السحب والإفلات لإعادة ترتيب الحزم والبنود في
   apps/web/src/components/ScopeBuilder.tsx.

الأول أعلى قيمة من الثاني بفارق كبير — القالب يوفّر عملًا متكررًا حقيقيًا،
والسحب والإفلات راحة. نفّذ الأول واعتبر الثاني اختياريًا.
```

---

## المرحلة ٥ — الذكاء الاصطناعي

> **حالة الوحدة كما كشفها الفحص، لا كما هي مسجَّلة:**
> - `AiRouterService` مسجَّل في `ai.module.ts` ويُصدَّر — **حي**.
> - `AiMemoryService` و`AiSuggestionService` مكتوبتان كملفين، **غير مسجَّلتين في الوحدة وغير مستدعاتين من أي موضع** — كود ميت.
> - **لا يوجد `@Controller` واحد في الوحدة** — صفر نقاط نهاية. لا شيء من هذا يصل المستخدم.
> - نموذج `AiSuggestion` في السكيما **مصمَّم جيدًا**: فيه `status` و`approverId` و`rejectionReason` و`confidenceScore` و`executionStatus`. أي أن دورة «اقتراح ← مراجعة بشرية ← تنفيذ» مصمَّمة بالفعل ولم تُبنَ.
> - `.env.example` يوجّه الافتراضات إلى **مزوّدين سحابيين** (gemini، openai، xai) بينما السؤال رقم ٦ عن سحابي أم داخلي **لم يُجَب**. توتر حقيقي يعالجه T-23.

### T-20 · توصيل وحدة الـAI الميتة

**لماذا:** خدمتان مكتوبتان لا يستدعيهما أحد. الكود الميت أسوأ من الغائب — يوهم بأن الميزة موجودة.

```
أعد توصيل الأجزاء الميتة في وحدة الذكاء الاصطناعي.

الحالة المؤكدة بالفحص: apps/api/src/ai/ai.module.ts يسجّل AiRouterService
وحده. بينما AiMemoryService في memory/ و AiSuggestionService في suggestions/
موجودتان كملفين، غير مسجَّلتين في الوحدة، وغير مستدعاتين من أي موضع في
المشروع كله.

اقرأ الملفين أولًا وقرّر بوعي — ولا تسجّل شيئًا لمجرد أنه مكتوب:
- إن كان المنطق فيهما سليمًا وموافقًا لنموذج AiSuggestion في السكيما:
  سجّلهما في ai.module.ts، وأصلح ما لا يتوافق، واكتب اختبارات وحدة لهما.
  الاختبار هنا ليس إجراءً شكليًا — كود لم يُستدعَ قط لم يُثبت أنه يعمل.
- إن كان أحدهما مبنيًا على تصميم متجاوَز: احذفه وسجّل سبب الحذف في
  PROGRESS.md. حذف كود ميت تقدّم لا تراجع.

راجع أيضًا ai-router.service.ts: تأكد أن سلوكه عند غياب مفتاح المزوّد
مفهوم وصريح — يفشل برسالة واضحة أو يتراجع إلى بديل، لا يرمي خطأً غامضًا.
مفاتيح المزوّدين في .env.example فارغة اليوم، فهذا هو المسار الأكثر احتمالًا
عند أول تشغيل.

لا تضف أي ميزة جديدة في هذا التاسك. المخرج: وحدة متماسكة، كل ما فيها حي
ومختبَر، ولا شيء فيها معلّق.
```

### T-21 · واجهة برمجية للمحادثة والاقتراحات

```
ابنِ الواجهة البرمجية لوحدة الذكاء الاصطناعي. اليوم لا يوجد أي @Controller
في apps/api/src/ai/ — أي أن الوحدة كلها غير قابلة للوصول من أي عميل.

النماذج جاهزة في السكيما: AiConversation و AiMessage و AiSuggestion. اقرأها
قبل أن تصمم أي شيء — فيها قرارات تصميمية مقصودة يجب احترامها، أهمها أن
AiMessage يحفظ task و provider و model لكل رد، وأن AiSuggestion يحمل دورة
حياة كاملة بـstatus و approverId و rejectionReason.

المطلوب:
١. نقاط نهاية للمحادثة: إنشاء محادثة، إرسال رسالة، قراءة السجل، حذف محادثة.
٢. نقاط نهاية للاقتراحات: قراءة المعلّق، اعتماد، رفض مع سبب.
٣. كل نداء يمر بالمسار عبر AiRouterService باستخدام AiTask المناسب من
   ai.types.ts. لا تكتب اسم مزوّد ولا اسم نموذج في أي مكان خارج الموجّه —
   هذا هو الغرض الكامل من وجوده، وكسره يعني أن تغيير المزوّد لاحقًا سيمسّ
   كل ملف.
٤. المحادثة خاصة بصاحبها. تأكد أن مستخدمًا لا يقرأ محادثة غيره — النموذج
   يربط userId، فاحترمه في كل استعلام لا في بعضها.

انتبه لثلاثة:
- انقطاع اتصال المزوّد يجب أن يعطي خطأً مفهومًا للمستخدم، لا انتظارًا طويلًا
  ثم فشلًا صامتًا. اضبط مهلة صريحة.
- لا تخزّن نص الطلب الكامل المُرسل للمزوّد — تعليق السكيما ينصّ صراحة على
  أن البيانات المحفوظة تبقى صغيرة. احترم القرار.
- التكلفة: كل نداء له ثمن. سجّل ما يكفي لمعرفة الاستهلاك لاحقًا.
```

### T-22 · شاشة مراجعة الاقتراحات

**لماذا:** نموذج `AiSuggestion` مصمَّم أصلًا حول مراجعة بشرية — `status` و`approverId` و`rejectionReason`. الشاشة هي ما يجعل هذا التصميم حقيقيًا.

```
ابنِ شاشة مراجعة اقتراحات الذكاء الاصطناعي.

المبدأ الحاكم، وهو مبنيّ في السكيما لا مقترح مني: الذكاء الاصطناعي في هذا
النظام يقترح ولا ينفّذ. نموذج AiSuggestion يحمل status و approverId و
approvedAt و rejectionReason و executionStatus — أي أن دورة «اقتراح ← مراجعة
بشرية ← تنفيذ» مصمَّمة بالفعل. هذه الشاشة تجعلها حقيقية.

اقرأ النموذج في prisma/schema.prisma و apps/api/src/ai/suggestions/ قبل
البدء.

المطلوب: شاشة تحت apps/web/src/app/[locale]/(app)/ تعرض الاقتراحات المعلّقة،
ولكل اقتراح: ما اقترحه الوكيل (recommendation)، وما كانت البيانات قبله
(originalData)، وما التغيير المقترح بالضبط (suggestedChange)، ودرجة الثقة،
وأي وكيل أنتجه (agentKey) وعلى أي كيان.

ثلاثة قرارات تصميمية لا تتنازل عنها:
- الفرق قبل/بعد يجب أن يكون مرئيًا بوضوح. مراجع لا يرى ما سيتغيّر بالضبط
  لا يراجع، بل يوافق.
- درجة الثقة تُعرض ولا تُخفي شيئًا. اقتراح بثقة منخفضة يُعرض بوضوح أنه كذلك،
  ولا يُخفى بحجة تنظيف الشاشة.
- الرفض يتطلب سببًا. الحقل موجود في النموذج لغرض، وسبب الرفض هو ما سيحسّن
  الوكلاء لاحقًا.

راجع apps/api/src/ai/suggestions/approver-roles.ts — فيه تعريف من يحق له
الاعتماد، فاحترمه ولا تخترع صلاحية جديدة.
```

### T-23 · حاجز الخصوصية للمستندات

**لماذا:** `.env.example` يوجّه الافتراضات إلى مزوّدين سحابيين، بينما قرار Afro بشأن المستندات السرية لم يُتخذ. اليوم لا يوجد ما يمنع إرسال مستند سري إلى واجهة خارجية.
**يعتمد على:** T-03 وقرار Afro (سؤال ٦).

```
ابنِ حاجز الخصوصية بين مستندات ACMS ومزوّدي الذكاء الاصطناعي الخارجيين.

المشكلة الحقيقية: .env.example يوجّه الطبقات الثلاث افتراضيًا إلى مزوّدين
سحابيين — gemini و openai و xai — بينما السؤال رقم ٦ في PROGRESS.md عن
سياسة الذكاء الاصطناعي مع المستندات السرية لم يُجَب. أي أنه لا يوجد اليوم أي
شيء في الكود يمنع إرسال محتوى مستند سري إلى واجهة خارجية.

المعمار جيد ويستوعب الحل: AiRouterService يفصل المتصل عن المزوّد، وطبقة
providers/ فيها ai-provider.interface.ts. القرار يحدد المزوّد لا المعمار.

اعمل الآتي — والجزء الأول قابل للتنفيذ الآن بلا انتظار قرار Afro:

١. تصنيف حساسية على Document في السكيما، وسياسة صريحة: أي تصنيف يجوز إرساله
   لمزوّد خارجي وأيها لا. الافتراضي للمستند غير المصنَّف هو المنع لا السماح.

٢. فرض السياسة في AiRouterService نفسه لا في المتصلين. المتصل قد ينسى؛
   الموجّه لا يُتجاوَز. محاولة إرسال محتوى ممنوع تفشل بخطأ صريح يسمّي السبب،
   ولا تُمرَّر بصمت.

٣. مزوّد محلي في providers/ خلف نفس الواجهة (Ollama أو ما يعادله)، حتى يكون
   الخيار الداخلي جاهزًا للتشغيل لحظة اتخاذ القرار لا بعده بأسبوعين.

٤. أضف إلى docs/ صفحة تشرح ما يخرج من الشبكة وما لا يخرج بالضبط. هذه هي
   الوثيقة التي ستُسأل عنها Afro، ووجودها يسرّع القرار.

انتبه لتقاطع خطير: ما تخفيه طبقة ABAC (T-03) عن مستخدم يجب ألا يصل إلى
المزوّد ضمن سياق طلب ذلك المستخدم — وإلا سرّبه الذكاء الاصطناعي في رده.
تحقّق من هذا صراحةً وباختبار مخصص، فهو المسار الأكثر احتمالًا للتسريب.
```

### T-24 · أول وكيل فعلي — فحص التكلفة

**لماذا:** البنية كلها بلا وكيل واحد يعمل تبقى وعدًا. وفحص التكلفة أفضل بداية: البيانات مهيكلة، والخطأ مكلف، والاقتراح قابل للتحقق موضوعيًا.

```
ابنِ أول وكيل ذكاء اصطناعي فعلي في ACMS: وكيل فحص التكلفة.

لماذا هذا الوكيل أولًا لا غيره: بياناته مهيكلة بالكامل في قاعدة البيانات لا
نصًا حرًا، وخطأ التسعير مكلف فعليًا، والاقتراح قابل للتحقق موضوعيًا — يمكن
معرفة إن كان صائبًا. وكيل يكتب نصًا حرًا لا يمكن الحكم عليه، فيبقى انطباعًا.

ما يفعله: يقرأ سيناريو تكلفة ويرفع ما يستحق نظرة بشرية. أمثلة: بند تكلفته
شاذة مقارنة ببنود مشابهة في فرص سابقة، هامش تحت الحد المعتمَد في الإعدادات،
كمية لا تتناسب مع النطاق، أو بند في النطاق بلا ما يقابله في التكلفة —
والأخير أخطرها لأنه نقص صامت.

قواعد التنفيذ:
١. المخرج اقتراح AiSuggestion بحالة PENDING. الوكيل لا يعدّل بيانات أبدًا،
   ولا في أي حالة. القرار للبشر عبر شاشة T-22.
٢. agentKey ثابت ومعرَّف، و entityType = "CostingScenario"، وتعبئة
   originalData و suggestedChange بما يجعل الفرق قابلًا للعرض في الشاشة.
٣. درجة ثقة حقيقية مبنية على معيار، لا رقمًا ثابتًا ولا رقمًا يخترعه النموذج
   عن نفسه. ثقة بلا أساس أسوأ من غياب الثقة لأنها تُصدَّق.
٤. استخدم AiTask.REASONING عبر الموجّه لهذا النوع من التحليل، ولا تكتب اسم
   مزوّد.
٥. الحسابات الرقمية تُجرى في الكود لا في النموذج اللغوي. النموذج يقرأ أرقامًا
   محسوبة ويفسّرها ويرتّب أولوياتها — ولا يُطلب منه حساب هامش أو مقارنة أرقام.
   هذه أكثر نقطة يُخطئ فيها هذا النوع من الوكلاء.

ابدأ بحالة واحدة فقط — أقترح «بند نطاق بلا تكلفة مقابلة» لأنها الأوضح
والأسهل تحققًا — وأثبتها كاملة على السيرفر قبل إضافة الحالة الثانية. وكيل
واحد يعمل بثقة خير من خمسة لا يُوثق بهم.
```

### T-25 · تدقيق وحوكمة الـAI

```
اجعل استخدام الذكاء الاصطناعي في ACMS قابلًا للتدقيق والضبط.

نظام يحمل قرارات تجارية لا يجوز أن يكون فيه مسار غير مسجَّل، والذكاء
الاصطناعي هو المسار الأحدث والأقل وضوحًا فيه.

المطلوب:
١. كل نداء لمزوّد يُسجَّل في AuditLog: من طلبه، أي وكيل، على أي كيان، أي
   طبقة وأي مزوّد ونموذج — النموذج يحفظ task و provider و model بالفعل في
   AiMessage، فاستفد منها ولا تكرّر تخزينها.
٢. كل اعتماد أو رفض لاقتراح يُسجَّل مع سببه. هذا هو الأثر الذي سيُسأل عنه
   حين ينفَّذ اقتراح خاطئ.
٣. حدود استهلاك قابلة للضبط من شاشة الإعدادات: حد لكل مستخدم وحد إجمالي.
   بلا حد، خطأ في حلقة تكرار يصبح فاتورة. اتبع نمط شاشة الإعدادات القائم في
   ApprovalPolicySettings.tsx — إعدادات لا ثوابت في الكود، وهو مبدأ مطبَّق
   بالفعل في هذا المشروع.
٤. مفتاح إيقاف عام للذكاء الاصطناعي. عند إطفائه يتوقف كل نداء خارجي فورًا
   ويبقى النظام كاملًا صالحًا للعمل. هذا شرط ألا يصير الذكاء الاصطناعي نقطة
   فشل لنظام لم يكن يعتمد عليه أصلًا.

أضف فحوصًا في smoke.sh: أن الإيقاف يوقف فعلًا، وأن تجاوز الحد يُرفض برسالة
مفهومة لا بخطأ عام.
```

---

## المرحلة ٦ — ما ليس بيدنا

### T-26 · الأسئلة الأربعة إلى Afro

**لماذا:** الانتظار يبدأ اليوم لا حين نفرغ. والسؤال ٤ يعطّل عملًا مجدولًا في المرحلة الثانية.

```
جهّز رسالة واحدة إلى Afro Group بالأسئلة الأربعة المتبقية، مرتّبة حسب ما
يعطّله كل سؤال لا حسب ترقيمها في PROGRESS.md.

الترتيب المطلوب:
١. سؤال ٤ — عدد الكيانات القانونية والدول. الأكثر إلحاحًا لأنه يعطّل T-06
   المجدول في المرحلة الثانية، ويؤثر على تصميم T-15.
٢. سؤال ٧ — هل توجد واجهات برمجية لـNama ERP وأنظمة العمليات أصلًا. يعطّل
   الإصدارات ٨ و٩ و١٠ بالكامل.
٣. سؤال ٦ — سياسة الذكاء الاصطناعي مع المستندات السرية: سحابي أم داخلي.
   يحدّد T-23.
٤. قرار البوابات لمستخدمين من خارج الشركة. يؤثر على مصفوفة T-02 مباشرة —
   إن كان للموردين حسابات فالمصفوفة تتغيّر جذريًا.

لكل سؤال اكتب سطرًا واحدًا فقط: ما الذي يتوقف انتظارًا لهذه الإجابة. لا
تشرح الخلفية التقنية — من سيقرأ الرسالة يريد أن يعرف ثمن تأخره لا كيف يعمل
النظام.

للسؤال رقم ٧ تحديدًا: docs/integration-request.md جاهز ومفصّل بأسئلته السبع.
أشر إليه ولا تعد كتابته. المطلوب هناك متابعة لا تأليف.

أضف في نهاية الرسالة سؤالًا مباشرًا: من هو الشخص المسؤول عن كل إجابة ومتى
نتوقعها. سؤال بلا صاحب لا يُجاب.
```

---

## ملاحظات على التنفيذ

**ابدأ اليوم بهذه الثلاثة معًا:** T-08 ثم T-09 و T-10 (يوم واحد، بلا مساس بأي منطق، وأثرها فوري على الانطباع)، و T-26 بالتوازي لأن الانتظار يجب أن يبدأ من الآن.

**التبعيات التي كسرها مكلف:**
- `T-03 ← T-11` — التخزين المؤقت قبل ABAC يخرق الإخفاء
- `T-02 ← T-03` — ABAC بلا مصفوفة معتمدة تخمين
- `T-03 ← T-23` — حاجز الـAI يجب أن يحترم ما تخفيه ABAC
- `سؤال ٤ ← T-06` — تعدد الدول يغيّر شكل الشاشة نفسها

**قابل للتنفيذ المتوازي بلا انتظار أحد:** T-09، T-10، T-13، T-15، T-16، T-20.

**أول ما يسقط عند ضيق الوقت، بهذا الترتيب:** T-19 ثم T-18. كلاهما تحسين تجربة لشاشة تعمل اليوم.

**لا يدخل قائمة العمل إطلاقًا:** `Solution Strategy` — بند معلن في فهرس الإصدار الثالث بلا جدول ولا شرح في أي موضع من الوثيقة، وغير قابل للبناء بلا توضيح. موضعه قائمة الأسئلة لا قائمة التاسكات.
