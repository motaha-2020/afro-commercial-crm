node_modules/
dist/
build/
.next/
out/
coverage/

.env
.env.*
!.env.example

*.log
npm-debug.log*
.DS_Store
Thumbs.db

.vscode/
.idea/

# Never commit source media / client documents
*.mp4
*.docx
*.pdf
~$*

# The staging copy of the whole project lives here for convenience. It is not
# a second source of truth and must never be committed — two copies of the same
# tree in one repository leaves nobody sure which one is real.
acms-agent/
ACMS · AI Agent.json

# Incremental build cache — regenerated on every build.
*.tsbuildinfo
